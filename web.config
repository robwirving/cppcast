<?xml version="1.0" encoding="UTF-8"?>
<configuration>
<!--
    This is a dummy file used to prevent the azure deployment
    script from trying to create its own web.config file.
-->
</configuration>
